import 'package:flutter/material.dart';

class InspectionLocationSection extends StatelessWidget {
  const InspectionLocationSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
