import 'package:flutter/material.dart';

class InspectionConditionSection extends StatelessWidget {
  const InspectionConditionSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
